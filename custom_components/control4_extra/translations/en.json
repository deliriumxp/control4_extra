{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "error": {
      "cannot_connect": "Failed to connect to the Control4 director at {host}",
      "controller_not_found": "No Control4 controller found on this account",
      "director_auth_failed": "The Control4 director rejected the authentication token",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "step": {
      "user": {
        "data": {
          "host": "[%key:common::config_flow::data::ip%]",
          "password": "[%key:common::config_flow::data::password%]",
          "username": "[%key:common::config_flow::data::username%]"
        },
        "description": "Please enter your Control4 account details and the IP address of your local controller. You'll choose which entity types to import on the next screen (Configure)."
      }
    }
  },
  "entity": {
    "climate": {
      "thermostat": {
        "state_attributes": {
          "fan_mode": {
            "state": {
              "circulate": "Circulate"
            }
          }
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "enabled_platforms": "Entity types to import from Control4",
          "dry_contact_covers": "Dry-contact covers (no position feedback)",
          "scan_interval": "Seconds between updates"
        },
        "description": "Only the selected entity types are created in Home Assistant. Unselecting a type removes its entities after a reload. For covers with no real position feedback (relay/dry-contact drivers), mark them below so the open/close buttons stay usable even when Control4's reported status already says fully open/closed."
      }
    }
  }
}
